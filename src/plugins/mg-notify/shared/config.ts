import { MGNotification } from './interface.js';

export const MGNotifications: MGNotification = {
    debug: false,
    sounds: true,
    labelSound: true,
    enableRebarSelector: true,
    duration: 10000,
    darkMode: false,
    position: 'right-center',
    textlabelPosition: 'right-center',
};
